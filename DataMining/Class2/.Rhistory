a <- c( 1, 2, 3 )
a
a <- ( 1, 2, 3 )
a
getwd()
x = c( 1, 2, 3, 4, 5 )
mean(x)
x = c( 1, 2, 3, 4 )
mean(x)
mean(x = c( 1, 2, 3, 4) )
x
mean(x = c( 1, 2, 3, 4) )
x
mean(x = c( 1, 2, 3, 4) )
x
mean(x <- c( 1, 2, 3, 4) )
x
x<-NA
rm( x )
mean(x = c( 1, 2, 3, 4) )
x
x                            # x is in kernel
df <- mtcars[,]
mean(x <- c( 1, 2, 3, 4) )
x                            # x is in kernel
df <- mtcars[,c(1:3, 7)]
df
View(df)
names( df )
x <- 1
rm( x ) # remove x from kernel
x       # error x is not in kernel
vec = c( names( df ) )
c
vec = c( mtcars$mpg, mtcars$hp )
vec
vec = c( mtcars$mpg, mtcars$hp, mtcars$cyl )
vec
vec = c( "mpg", "hp", "cyl" )
vec
mtcars( vec )
new_df <- mtcars[ vec ]
new_df
vec = c( "mpg", "hp", "cyl" )
new_df <- mtcars[ vec ]
new_df
min( mtcars$mpg )
which.min( mtcars$mpg )
which.min( mtcars$mpg )  # returns the number of the row of the column mpg
mtcars[15,]
mtcars[15][:]
mtcars[15, :]
mtcars[15]
mtcars[15, ]
mtcars[15:16, ]
max( mtcars$hp )        # returns minimum value from the column mpg
which.max( mtcars$hp )  # returns the number of the row of the column mpg
rownames( mtcars )
row <- which.max( mtcars$wt )
row
rownames( mtcars )[row]
rownames( mtcars[row] )
rownames( mtcars )[row]
rownames( mtcars )[ which.max( mtcars$wt ) ]
blya <- c( 1, 2, 3 )
blya <- c( range( 1, 25 ) )
blya
blya <- c( 1:25 )
blya
blya <- c( 1:25, 77 )
blya
mean( blya )
median( blya )
sd( blya )
range( blya )
range( blya, na.rm = TRUE )
range( blya, na.rm = FALSE)
range( blya )
a[2] - a[1]
a <- range( blya )
a
b = a[2] - a[1]
b
mn, mx <- a
summary( mtcars )
summary( 'Census R.csv' )
vecto<c(5,7,8)# assinging value to the object on the left
a<c(5,7,8)# assinging value to the object on the left
a<-c(5,7,8)# assinging value to the object on the left
a
mean(x=c(5.8.9.6.7))
mean(x=c(5,8,9,6,7))
mean(x<-c(5,8,9,6,7))
data.mtcars
data(mtcars)
new_df<-mtcars(c[1:3,7])
new_df<-mtcars[,c(1:3,7)]
View(mtcars)
new_df<-mtcars[,c(1:3,7)]
dim(new_df)
names(mtcars)
new_dfl<-mtcars[,c("mpg", "hp", "wt")]
names(mtcars)
new_dfl
names(new_dfl)
min(mtcars$mpg)
which.min(mtcars$mpg)
mtcars[15,]
mtcars[15,1]
mtcars[15,]
which.max(mtcars$hp)
rownames(mtcars)
rownames(mtcars[which.max(mtcars$hp),])
mean(mtcars$hp)
median(mtcars$HP)
median(mtcars$hp)
range(mtcars$hp) #max-min
range(mtcars$hp)[2] - range(mtcars$hp)[1] #max-min
range(mtcars$hp)([2]-[1])
range(mtcars$hp)[2] - range(mtcars$hp)[1]
max(mtcars$hp) - min(mtcars$hp)
summar(mtcars)
summary(mtcars)
summary(mtcars$hp)
summary(mtcars[,1:5])
file <- read.csv( 'Census R.csv' )
file
View( file )
View( file )
data <- read.csv( 'Census R.csv' )
View( data )
View( data )
data$sex <- factor( data$sex, levels = c( 1, 2 ), labels = c( 'Male', 'Female' ) )
View( data )
summary( data$sex )
data$happy <- factor( data$happy, levels = c( 1, 2, 3 ), labels = c( 'Very happy', 'Pretty happy', 'Not too happy' ) )
View( data )
data$happy <- factor( data$happy, levels = c( 1, 2, 3 ), labels = c( 'Very happy', 'Pretty happy', 'Not too happy' ), exclude = c( 8, 9 ) )
View( data )
data$happy <- ordered( data$happy, levels = c( 1, 2, 3 ), labels = c( 'Very happy', 'Pretty happy', 'Not too happy' ), exclude = c( 8, 9 ) )
View( data )
data <- read.csv( 'Census R.csv' )
data$sex <- factor( data$sex, levels = c( 1, 2 ), labels = c( 'Male', 'Female' ) )
data$happy <- ordered( data$happy, levels = c( 1, 2, 3 ), labels = c( 'Very happy', 'Pretty happy', 'Not too happy' ), exclude = c( 8, 9 ) )
View( data )
data[ data$age %int% c(98,99) ] <- NA
data$age[ data$age == 99 ] <- NA
data$age[ data$age == 98 ] <- NA
data[ data$age %int% c(98,99) ] <- NA
summary( data$age )
avarage( data$age )
mean( data$age )
mean( data$age )
mean( data$age, na.rm = TRUE )
sd( data$age, na.rm = TRUE )
median( data$age, na.rm = TRUE )
new_data <- data[ data$age > 34 ]
new_data <- data[, data$age > 34 ]
new_data <- data[:, data$age > 34 ]
new_data <- data[, data$age > 34 ]
new_data <- data[data$age > 34, ]
new_data
View( new_data )
summary( new_data$age )
new_data <- data[data$age > 34 || data$age < 19, ]
summary( new_data$age )
summary( data )
summary( data$age )
aggregate( data, by = list( data$age ), FUN = 'mean', na.rm = TRUE )
aggregate( data$age, by = list( data$age ), FUN = 'mean', na.rm = TRUE )
aggregate( data$age, by = list( data$sex ), FUN = 'mean', na.rm = TRUE )
aggregate( data$age, by = list( data$sibs ), FUN = 'mean', na.rm = TRUE )
aggregate( data[, c( data$age, dat$sibs ) ], by = list( data$sex ), FUN = 'mean', na.rm = TRUE )
aggregate( data[, c( data$age, data$sibs ) ], by = list( data$sex ), FUN = 'mean', na.rm = TRUE )
aggregate( data[, c( 'age', 'sibs') ], by = list( data$sex ), FUN = 'mean', na.rm = TRUE )
aggregate( data[, c( 'age', 'sibs') ], by = list( data$sex, data$happy ), FUN = 'mean', na.rm = TRUE )
table( data$sex )
table( data$sex, data$happy )
blah <- table( data$sex, data$happy )
prop.table( blah )
prop.table( blah, 1 )
aggregate( data[, c( 'age', 'sibs') ],
by = list( data$sex, data$happy ),
FUN = 'mean',
na.rm = TRUE )
